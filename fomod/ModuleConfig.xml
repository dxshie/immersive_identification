<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="http://qconsulting.ca/fo3/ModConfig5.0.xsd">
	<moduleName>Immersive Identification</moduleName>

	<!-- The base mod is a single self-contained gamedata drop-in with no
	     required choices; the wizard step below is for optional
	     compatibility patches / integrations. -->
	<requiredInstallFiles>
		<folder source="gamedata" destination="gamedata" />
	</requiredInstallFiles>

	<installSteps order="Explicit">
		<installStep name="Optional Components">
			<optionalFileGroups order="Explicit">
				<group name="Compatibility" type="SelectAny">
					<plugins order="Explicit">
						<plugin name="Neutralize FactionID HUD">
							<description>Disables the separate FactionID mod's own on-screen faction HUD (its script becomes a no-op) so it doesn't show its own indicator alongside this mod's -- this mod replaces that HUD with its world-anchored tag, so running both means two faction indicators at once. Enabled by default because most setups (GAMMA included) ship FactionID; it is inert if you do not have FactionID installed, and you can uncheck it to keep FactionID's HUD.</description>
							<files>
								<folder source="FactionID Neutralized/gamedata" destination="gamedata" />
							</files>
							<typeDescriptor>
								<type name="Recommended" />
							</typeDescriptor>
						</plugin>
						<plugin name="Skill System: Perception">
							<description>Adds a "perception" skill to the Skill System (haru_skills) mod and links it to identification speed: the higher your perception level, the faster you identify targets, and successful identifications grant perception XP. Only install this if you also have the Skill System mod installed.</description>
							<files>
								<folder source="Perception Skill Integration/gamedata" destination="gamedata" />
							</files>
							<typeDescriptor>
								<type name="Optional" />
							</typeDescriptor>
						</plugin>
						<plugin name="Wearable Devices Compatibility">
							<description>Gates identification behind wearable scanner gear from the st-wearable-devices mod. Adds a Promin Antenna module, a 3-tier Promin Process module, and a 3-tier worn Identification Scanner. With the full kit assembled (bracer worn + Promin with antenna and a process module + a worn scanner), identification runs on a tier system: the process tier sets scan speed and gradually unlocks the data shown (faction, distance, relationship, rank, weapon); the scanner tier sets range and unlocks scope/ADS support, a magnification range boost, and no-night-penalty. Tune every tier value on the mod's "Wearable Devices" MCM page. Only install this if you also have st-wearable-devices installed. Uses placeholder item art/models.</description>
							<files>
								<folder source="WD Compatibility/gamedata" destination="gamedata" />
							</files>
							<typeDescriptor>
								<type name="Optional" />
							</typeDescriptor>
						</plugin>
					</plugins>
				</group>

				<!-- Separate group, and SelectAtMostOne: both plugins register the SAME
				     ii_patch_* ids over the SAME atlas file name, so ticking both would leave
				     whichever parsed last silently winning. They differ only in tile geometry,
				     because the two mods ship different atlases under that one name. -->
				<group name="Faction patches (pick the one matching your atlas)" type="SelectAtMostOne">
					<plugins order="Explicit">
						<plugin name="GRIP Patches">
							<description>Uses Kos' GRIP (GAMMA Remastered Icon Pack) faction patch artwork for the tag's faction emblem instead of the game's own faction icons. GRIP: https://discord.com/channels/912320241713958912/1530072690155585688/1530072690155585688 -- its atlas is 1024x512 with 128x128 tiles. This component only registers the mod's ii_patch_* ids as regions of that atlas; no texture is copied, only referenced, so it needs GRIP installed and does nothing without it. PICK ONLY ONE patch component: G.A.M.M.A. UI ships a DIFFERENT atlas under the same file name and the same texture ids (512x256, 64x64 tiles), so choosing the wrong one renders every patch cropped to a corner. You must also switch on "Custom faction patches" on the mod's UI Style / General MCM page; the component alone changes nothing. Covers every faction in the atlas (zombified stalkers reuse its greyed stalker patch); trader, monster and arena have no tile there and show no patch.</description>
							<files>
								<folder source="GRIP Patches/gamedata" destination="gamedata" />
							</files>
							<typeDescriptor>
								<type name="Optional" />
							</typeDescriptor>
						</plugin>
						<plugin name="GAMMA Patches">
							<description>Uses the G.A.M.M.A. UI faction patch artwork for the tag's faction emblem instead of the game's own faction icons. Its atlas is 512x256 with 64x64 tiles. This component only registers the mod's ii_patch_* ids as regions of that atlas; no texture is copied, only referenced, so it needs the G.A.M.M.A. UI mod installed and does nothing without it. PICK ONLY ONE patch component: Kos' GRIP ships a DIFFERENT atlas under the same file name and the same texture ids (1024x512, 128x128 tiles), so choosing the wrong one renders every patch cropped to a corner. If both mods are installed, pick the one your load order lets win. You must also switch on "Custom faction patches" on the mod's UI Style / General MCM page; the component alone changes nothing. Covers every faction in the atlas (zombified stalkers reuse its greyed stalker patch); trader, monster and arena have no tile there and show no patch.</description>
							<files>
								<folder source="GAMMA Patches/gamedata" destination="gamedata" />
							</files>
							<typeDescriptor>
								<type name="Optional" />
							</typeDescriptor>
						</plugin>
					</plugins>
				</group>
			</optionalFileGroups>
		</installStep>
	</installSteps>
</config>
